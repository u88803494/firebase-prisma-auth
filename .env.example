# ===========================================
# Firebase + Prisma Auth - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# cp .env.example .env.local

# -------------------------------------------
# Firebase Frontend SDK (Required)
# -------------------------------------------
# Get from: Firebase Console > Project Settings > General > Your apps > Web
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# -------------------------------------------
# Firebase Admin SDK (Required - Choose One)
# -------------------------------------------
# Option A: Service Account Key (Recommended - Never expires)
# Get from: Firebase Console > Project Settings > Service accounts > Generate new private key
# Compress JSON to single line, wrap with single quotes
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"...","private_key":"..."}'

# Option B: Application Default Credentials (Expires ~1 hour)
# Run: gcloud auth application-default login
# No environment variable needed, but requires gcloud CLI

# -------------------------------------------
# Database (Required)
# -------------------------------------------
# IMPORTANT: Must use absolute path for SQLite
# Example: file:/Users/yourname/projects/firebase-prisma-auth/prisma/dev.db
# Get absolute path: pwd
DATABASE_URL="file:/absolute/path/to/prisma/dev.db"

# -------------------------------------------
# JWT Secret (Required)
# -------------------------------------------
# Generate with: openssl rand -base64 32
JWT_SECRET=

# -------------------------------------------
# Test Phone Numbers (Firebase Console Setup)
# -------------------------------------------
# These must be configured in Firebase Console:
# Authentication > Sign-in method > Phone > Phone numbers for testing
#
# Phone: +886900000001  OTP: 111111
# Phone: +886900000002  OTP: 111111
# Phone: +886900000003  OTP: 111111
