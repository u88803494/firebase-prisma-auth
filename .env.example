# ================================
# Firebase 前端 SDK 配置
# ================================
# 從 Firebase Console > 專案設定 > 一般 > 您的應用程式 取得
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ================================
# Firebase Admin SDK 認證
# ================================
# 方法 1（推薦）：使用 Service Account Key JSON（壓縮成一行）
# 從 Firebase Console > 專案設定 > 服務帳戶 > 產生新的私密金鑰
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"your-project-id",...}'

# 方法 2（替代）：使用 Application Default Credentials (ADC)
# 執行以下指令後，此變數可省略：
# gcloud auth application-default login
# gcloud config set project your-project-id

# ================================
# 資料庫配置
# ================================
# 開發環境：使用 SQLite（絕對路徑）
DATABASE_URL="file:/absolute/path/to/firebase-prisma-auth/prisma/dev.db"

# 生產環境：使用 PostgreSQL
# DATABASE_URL="postgresql://username:password@localhost:5432/database_name"

# ================================
# OAuth Provider 配置
# ================================
# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Facebook OAuth
FACEBOOK_APP_ID=your_facebook_app_id
FACEBOOK_APP_SECRET=your_facebook_app_secret

# LINE Login
LINE_CHANNEL_ID=your_line_channel_id
LINE_CHANNEL_SECRET=your_line_channel_secret

# ================================
# JWT 配置（可選）
# ================================
# 用於自訂 JWT Token 簽署（如果專案需要）
# JWT_SECRET=your_jwt_secret_here
